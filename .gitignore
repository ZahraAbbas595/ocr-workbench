# Python cache and virtual environment
__pycache__/
*.pyc
.venv/

# Secrets — never commit the real .env file
.env

# Ignore generated OCR results, but keep the folder
results/*
!results/.gitkeep
# Private/internal documents — never commit these
samples/Best_Coding_Practices*.pdf